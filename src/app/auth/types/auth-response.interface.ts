import { CurrentUserInterface } from "src/app/shared/types/current-user.interface";

/* Interface used for register and login responses */
export interface AuthResponseInterface {
    user: CurrentUserInterface
}